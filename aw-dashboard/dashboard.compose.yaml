version: "3"

services:

  metabase:
    container_name: metabase
    image: metabase/metabase:v0.56.5.4
    volumes:
      - ./conf/metabase:/metabase.db
    ports:
      - 3030:3000
    networks:
      - dashboard_network

networks:
  dashboard_network:
    external:
      name: ${DOCKER_NETWORK}